h1 listUsers
a(href="/users/create") New
style.
  th,td{
  border: 1px soild black;
  text-align:center;
  }
  table{
    width: 500px;
  }
table
      tr
        th Name
        th Age  
        th UPDATE
        th DELETE
      each user in users   
        tr
          td=user.name
          td=user.age
          td
            a(href="/books/"+user.id+"/update") Update
          td
            a(href="/books/"+user.id+"/delete") Remove